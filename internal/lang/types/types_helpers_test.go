// Types helpers tests — shared test helpers for types/ tests.
// Does NOT cover: direct feature tests (see adjacent test files).
package types

// testOps is the shared TypeOps instance for all test files in this package.
var testOps = &TypeOps{}
