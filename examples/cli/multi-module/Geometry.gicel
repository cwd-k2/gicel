-- Geometry module: Point type and operations.

form Point := { MkPoint: Int -> Int -> Point }

mkPoint :: Int -> Int -> Point
mkPoint := \x y. MkPoint x y

getX :: Point -> Int
getX := \p. case p { MkPoint x _ => x }

getY :: Point -> Int
getY := \p. case p { MkPoint _ y => y }
