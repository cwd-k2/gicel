# Codebase Audit — 残存項目

原本: 2026-03-29、Part I (A-T) + Part II (P1-P5)。
Part II は完了。Part I は以下が残存。

---

## Finding Q: `checkDo` の V8 ワークアラウンド

`elaborate_do_monadic.go` に 5 分岐パスがあり、うち 2 つが「V8 fix」と明示されたバイパス。

---

### 残存する `"<error>"` sentinel（低優先度）

パーサの `expectUpper()`/`expectLower()` が `"<error>"` 文字列を返す。
これらは `parse_import.go` の `ImportName{Name: "<error>"}` に伝搬する。
checker には到達しない（`lookupVar`/`lookupCon` のガードは除去済み）。
